import { SortByPopulationPipe } from './sort-by-population.pipe';

describe('SortByPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
